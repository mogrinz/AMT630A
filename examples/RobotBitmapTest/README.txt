RobotBitmapTest
===============

This example includes robot.h, which defines the AMT630A_OSD::Bitmap object
named `robot`. The example loads the bitmap once, places it at the bottom of
the configured display, then animates it left/right with moveWindow().